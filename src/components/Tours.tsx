// import tours from "./tour"
// import Package from "../partials/Package"

const Tours = () => {
  return (
    <section>
        {
            // tours.map(p => )
        }
    </section>
  )
}

export default Tours